import { SiteConfig } from "@/types";

export const siteConfig: SiteConfig = {
  mainNav: [
    {
      title: "Home",
      href: "/home",
      disabled: false,
    },
    {
      title: "Projects",
      href: "/projects",
      disabled: false,
    },
  ],
};
